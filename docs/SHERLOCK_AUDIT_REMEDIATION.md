# Sherlock audit remediation register

Every finding raised in the Sherlock audit and the follow-up review passes, with its disposition. Generated from the merged remediation branch and verified against [`fea5130`](https://github.com/moonwell-fi/mamo-contracts/commit/fea51302b610f64ddade261a9d83f1670494bea7).

- **60** remediated
- **69** accepted as residual, partially addressed, or deferred
- **8** raised and refuted

Line links are permalinks to the merge commit, so they stay correct as the branch moves.


## Remediated


### LP / Aerodrome

| Finding | Sev | How it was fixed |
|---|---|---|
| **Sherlock #51 / MOO-723** — Forced ETH donation to the shared Slipstream PositionManager bricks every mint (DoS) | High | Fixed in code by adding a payable receive(); any ETH that lands is sweepable by the admin via recoverETH.<br>[`src/LPAutoBalancerV2.sol:314`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/LPAutoBalancerV2.sol#L314) |
| **Sherlock #62 / MOO-727** — rebuildAfterSwap mints at live spot with no caller commitment — sandwichable range placement | High | Fixed in code: RebuildParams commits expectedTickLower/Upper and the derived range must match exactly; width constrained to a multiple of 2*tickSpacing so the commitment also pins the alt anchor on the balanced branch. Reviewer verified the check is not a recomputation (compares against the same locals _mintBalanced receives), is unconditional, and (0,0) can never match a derived range.<br>[`src/LPAutoBalancerV2.sol:960`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/LPAutoBalancerV2.sol#L960)<br>[`src/LPAutoBalancerV2.sol:1024`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/LPAutoBalancerV2.sol#L1024)<br>[`src/LPAutoBalancerV2.sol:954`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/LPAutoBalancerV2.sol#L954)<br>[`src/libraries/LPPositionLib.sol:86`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/libraries/LPPositionLib.sol#L86) |
| **Review #2 (imthatcarlos)** — rebalanceUsingAlt kept the unprotected placement MOO-727 closed on the rebuild path | Medium | Fixed in code by extending the commitment to RebalanceParams, plus a spec §2.2 update explaining that on this path the caller must predict the teardown output (principalAmounts(main)+principalAmounts(alt) at current sqrtP plus loose balance) rather than reading balanceOf.<br>[`src/LPAutoBalancerV2.sol:1120`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/LPAutoBalancerV2.sol#L1120)<br>[`src/LPAutoBalancerV2.sol:1003`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/LPAutoBalancerV2.sol#L1003)<br>[`src/LPAutoBalancerV2.sol:1107`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/LPAutoBalancerV2.sol#L1107) |
| **Review #3 (imthatcarlos)** — unwindForSwap read no oracle, so a stale feed or sequencer grace period was discovered only after both NFTs were burned | Medium | Fixed in code with an explicit gate carrying its own natspec, so a future change to the snapshot representation cannot delete the precondition by accident. Reviewer confirmed no same-transaction state exists where the probe passes and a rebuild feed read reverts.<br>[`src/LPAutoBalancerV2.sol:821`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/LPAutoBalancerV2.sol#L821)<br>[`src/LPAutoBalancerV2.sol:744`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/LPAutoBalancerV2.sol#L744)<br>[`src/LPAutoBalancerV2.sol:816`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/LPAutoBalancerV2.sol#L816) |
| **Sherlock #52 / MOO-729** — claimEmissions() was permissionless | Medium | Fixed in code by gating on REBALANCER_ROLE.<br>[`src/LPAutoBalancerV2.sol:651`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/LPAutoBalancerV2.sol#L651) |
| **Sherlock #53 / MOO-728** — isValidSignature remained live while the contract was paused | Medium | Fixed in code with the whenNotPaused modifier.<br>[`src/LPAutoBalancerV2.sol:1495`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/LPAutoBalancerV2.sol#L1495) |
| **Sherlock #55 / MOO-741** — No Base L2 sequencer uptime guard on Chainlink reads | Medium | Fixed in code AND wired in the deployment proposal. Honest scope caveat the extractor left implicit: the balancer's constructor does NOT seed sequencerUptimeFeed, so the guard is a no-op (checkSequencer early-returns on address(0)) until setSequencerUptimeFeed runs. Proposal 011 is also the contract's first deployment and arms it in the same build(), so there is no window where a deployed balancer reads feeds unguarded — but the guard's on-chain effect is contingent on 011 executing as written.<br>[`src/libraries/LPValuationLib.sol:77`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/libraries/LPValuationLib.sol#L77)<br>[`src/libraries/LPValuationLib.sol:95`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/libraries/LPValuationLib.sol#L95)<br>[`multisig/mamo-multisig/011_LPAutoBalancerV2Setup.sol:291`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/multisig/mamo-multisig/011_LPAutoBalancerV2Setup.sol#L291) |
| **Sherlock #56 / MOO-736** — CLGauge.deposit() collects pre-stake LP fees to the balancer, where they are indistinguishable from principal | Medium | Fixed in code: stake() skims fees to the feeCollector before each gauge deposit, matching _exitAll's existing skim-then-act ordering.<br>[`src/LPAutoBalancerV2.sol:527`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/LPAutoBalancerV2.sol#L527) |
| **Sherlock #59 + #61 / MOO-730** — USD value floor frozen across two transactions reads an ordinary market move as a rebalance loss; compound orders settleable mid-window | Medium | Fixed in code: the floor baseline is stored in token amounts and priced at rebuild time with the same feed read used for valueAfter; compound settlement is excluded for the duration of the swap window.<br>[`src/libraries/SwapWindowLib.sol:56`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/libraries/SwapWindowLib.sol#L56)<br>[`src/LPAutoBalancerV2.sol:945`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/LPAutoBalancerV2.sol#L945)<br>[`src/LPAutoBalancerV2.sol:188`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/LPAutoBalancerV2.sol#L188)<br>[`src/LPCompoundModule.sol:141`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/LPCompoundModule.sol#L141) |
| **Sherlock #60 / MOO-740** — A single 26-hour oracle staleness bound for two ~20-minute Chainlink feeds | Medium | Initially shipped structurally fixed but functionally open (the split was added but both legs still defaulted to 26 hours and no proposal ever called the setter). Closed in three places in 17a60de: tighter constructor default, tighter ceiling, and an explicit proposal action. NOTE ON SCOPE: the contract-side half (per-feed bounds, 1 h default, 1 day ceiling) is genuinely on-chain-effective the moment the contract is constructed; the 3600/3600 arming is in proposal 011, which has not executed — but since 011 also DEPLOYS the balancer for the first time, the pre-proposal state is the 1 h constructor default, so the finding is closed either way. Validated as empirically safe (3600 s ~ 2.9 heartbeats against a measured 1232 s worst case) and non-bricking (exit() is oracle-free).<br>[`src/LPAutoBalancerV2.sol:116`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/LPAutoBalancerV2.sol#L116)<br>[`src/LPAutoBalancerV2.sol:105`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/LPAutoBalancerV2.sol#L105)<br>[`src/LPAutoBalancerV2.sol:93`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/LPAutoBalancerV2.sol#L93)<br>[`src/LPAutoBalancerV2.sol:465`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/LPAutoBalancerV2.sol#L465) |
| **Sherlock #63 + #54 / MOO-742** — Alt leg anchored to the main range's bounds; token1-majority main shifted a spacing too far down | Medium | Fixed in code by anchoring the alt to spot and using a plain floor for the token1-majority main. Reviewer verified globally that a spot-straddling alt is now unreachable by construction: a token0 alt's tickLower is strictly above spot; a token1 alt's tickUpper is at or below it and range activity is tickLower <= tick < tickUpper.<br>[`src/libraries/LPPositionLib.sol:328`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/libraries/LPPositionLib.sol#L328)<br>[`src/libraries/LPPositionLib.sol:317`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/libraries/LPPositionLib.sol#L317) |
| **Validation-(a)** — Proposal 011's validate() could not detect the oracle-delay action's absence — 1 hours == 3600 == the constructor default | Medium | Fixed with a value-based rather than event-based pin: an event assertion would prove the setter was called, this proves the value it wrote survives to validate(), which is what MOO-740 is about. Mutation-verified by both parties — deleting _wireOracleDelays fails both new tests; moving it below registerPosition fails only the ordering test.<br>[`test/LPAutoBalancerV2Setup.integration.t.sol:303`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/test/LPAutoBalancerV2Setup.integration.t.sol#L303)<br>[`test/LPAutoBalancerV2Setup.integration.t.sol:340`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/test/LPAutoBalancerV2Setup.integration.t.sol#L340) |
| **Validation-(b)** — The unwind probe's position and its both-feeds property were untested — vm.expectRevert rolls the burn counter back | Medium | Fixed in tests. The old test's comment claiming a counter assertion 'pins that the guard sits ahead of the teardown' was wrong and was replaced. The control case is load-bearing — the reviewer's independent probe of neutering the sentinel fails exactly the control, proving the wire can actually fire.<br>[`test/LPAutoBalancerV2.unit.t.sol:3265`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/test/LPAutoBalancerV2.unit.t.sol#L3265)<br>[`test/LPAutoBalancerV2.unit.t.sol:3243`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/test/LPAutoBalancerV2.unit.t.sol#L3243) |
| **Validation-3** — The swap-and-pop justification in proposal 011 and the spec doc was factually wrong | Medium | Doc/comment-only fix; the ordering itself was left as-is and is harmless today (the three revoked factories sit at indices 1, 2, 5 — re-confirmed on-chain). The recovery procedure is now spelled out, with the reviewer's final nit taken: a fresh grant appends only for a non-member.<br>[`multisig/mamo-multisig/011_DeployMultiMarketSystem.sol:230`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/multisig/mamo-multisig/011_DeployMultiMarketSystem.sol#L230)<br>[`multisig/mamo-multisig/011_DeployMultiMarketSystem.sol:187`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/multisig/mamo-multisig/011_DeployMultiMarketSystem.sol#L187)<br>[`multisig/mamo-multisig/011_DeployMultiMarketSystem.sol:347`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/multisig/mamo-multisig/011_DeployMultiMarketSystem.sol#L347) |
| **Validation-4** — Spec doc claimed #41 was 'FIXED in the strategies' | Medium | Doc/scope correction. Consequence: the 011 index-0 assertion is more load-bearing than 'a live off-chain read', and must be kept in every future BACKEND_ROLE-mutating proposal until the fleet has migrated.<br>[`docs/STRATEGY_REGISTRY_SPEC.md:108`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/docs/STRATEGY_REGISTRY_SPEC.md#L108)<br>[`multisig/mamo-multisig/011_DeployMultiMarketSystem.sol:339`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/multisig/mamo-multisig/011_DeployMultiMarketSystem.sol#L339) |
| **Review #1 sub-point (setMaxOracleDelay singular)** — The retained single-argument setter silently flattened per-feed tuning | Low | Removed rather than kept: the ABI-compat justification was empty because MAMO_LP_AUTO_BALANCER_V2 is absent from addresses/8453.json (011 deploys it for the first time) and the contract is not proxied, so there is no prior ABI or live runbook to preserve. Both facts re-verified here.<br>[`src/LPAutoBalancerV2.sol:457`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/LPAutoBalancerV2.sol#L457) |
| **Review #5 (imthatcarlos)** — Comments described pre-PR behaviour this PR changed | Low | Documentation-only fix; no behaviour change. Reviewer verified the new text against live Base (positions(0) reverts 'ID', collect on a nonexistent token reverts) and verified the strong MIN_ALT_VALUE_USD claim globally rather than in the happy path.<br>[`src/LPAutoBalancerV2.sol:1212`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/LPAutoBalancerV2.sol#L1212)<br>[`src/LPAutoBalancerV2.sol:57`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/LPAutoBalancerV2.sol#L57) |
| **Review #6 (imthatcarlos)** — valueTokensInUsd is dead product code costing bytecode in a size-critical library | Low | Removed. It was dead because the oracle-free rebalanceAmountsBefore() replaced the USD window getters its all-zero short-circuit existed for; its self-resolving-decimals assertion is still covered via calmGate.<br>[`src/libraries/LPValuationLib.sol:1`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/libraries/LPValuationLib.sol#L1) |
| **Sherlock #57 / MOO-747** — positions() slot 5 decoded as Uniswap's uint24 fee instead of Slipstream's int24 tickSpacing | Low | Fixed in code. Reviewer independently confirmed the re-typing is index-compatible across every decoder in the tree.<br>[`src/interfaces/INonfungiblePositionManager.sol:113`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/interfaces/INonfungiblePositionManager.sol#L113)<br>[`src/libraries/LPPositionLib.sol:119`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/libraries/LPPositionLib.sol#L119) |
| **Sherlock #58 / MOO-743** — _exitAll left burned token IDs in storage | Low | Fixed in code. Follow-up review finding 5 caught that collectFees and getDecisionSnapshot still justified their guards with 'we still hold the burned ids'; those comments were corrected in 17a60de (the guards themselves remain correct because positions(0) reverts).<br>[`src/LPAutoBalancerV2.sol:1214`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/LPAutoBalancerV2.sol#L1214) |
| **Validation-nit-1** — Backend spec §5 still asserted the retracted alt-pinning claim | Low | Documentation-only. The intermediate claim that it had been fixed was refuted by the reviewer's re-grep; closed in the final commit along with the same retraction in the contract's own natspec (src/LPAutoBalancerV2.sol:880-890), LPPositionLib.validateRebalanceConfig (:62-68) and the LPGeometryLib fuzz test's rationale (test/LPGeometryLib.unit.t.sol:109-112) — all three re-verified present.<br>[`docs/superpowers/specs/2026-07-03-lp-auto-balancer-v2-backend-spec.md:376`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/docs/superpowers/specs/2026-07-03-lp-auto-balancer-v2-backend-spec.md#L376) |
| **Validation-nit-2** — Risk register still showed MOO-740 open and listed the removed singular setter | Low | Documentation-only sweep; reviewer confirmed remaining '26h' hits repo-wide are correctly historical.<br>[`docs/superpowers/specs/2026-07-02-lp-auto-balancer-v2-swap-rebalance-design.md:148`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/docs/superpowers/specs/2026-07-02-lp-auto-balancer-v2-swap-rebalance-design.md#L148)<br>[`docs/superpowers/specs/2026-07-02-lp-auto-balancer-v2-swap-rebalance-design.md:154`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/docs/superpowers/specs/2026-07-02-lp-auto-balancer-v2-swap-rebalance-design.md#L154) |
| **Validation-nit-3** — MIN_ALT_VALUE_USD unit assertion still cited the retracted straddling-alt rationale | Low | Documentation-only fix inside a test; reviewer verified the new text is accurate.<br>[`test/LPAutoBalancerV2.unit.t.sol:3544`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/test/LPAutoBalancerV2.unit.t.sol#L3544) |
| **Validation-nit-3b** — Mock teardown trip-wire natspec had the collect/decreaseLiquidity order backwards | Low | Comment-only correction, made in the final commit.<br>[`src/LPAutoBalancerV2.sol:1192`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/LPAutoBalancerV2.sol#L1192) |
| **Validation-nit-4** — The ordering test's re-pin proofing was only half-derived | Low | Test hardening only, applied in the final commit.<br>[`test/LPAutoBalancerV2Setup.integration.t.sol:340`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/test/LPAutoBalancerV2Setup.integration.t.sol#L340) |
| **Validation-nit-5** — _refreshPriceFeeds was described as a re-publish when vm.mockCall freezes updatedAt | Low | Documentation-only; the decision not to widen the bounds in this suite was upheld as correct because the suite's job is to assert the values the proposal actually ships.<br>[`test/LPAutoBalancerV2Setup.integration.t.sol:365`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/test/LPAutoBalancerV2Setup.integration.t.sol#L365) |
| **Final-review nit 5 (011 action lettering)** — Proposal 011's header letters the actions (a)-(d) while the in-body PRECONDITION note used a different scheme | Informational | Documentation-only; extractor missed it. Flagged as 'trivial, half fixed' in the final review and finished in the last commit. No behaviour change and no bearing on any security claim.<br>[`multisig/mamo-multisig/011_LPAutoBalancerV2Setup.sol:42`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/multisig/mamo-multisig/011_LPAutoBalancerV2Setup.sol#L42) |

### Multi-market strategy & registries

| Finding | Sev | How it was fixed |
|---|---|---|
| **Sherlock #32 (+ #49) / MOO-724** — Compound fee bypassable — Merkl `claim` is permissionless, so rewards arrive without passing through `claimRewards` | High | First pass moved the fee to claim time as the audit recommended; an adversarial pass then produced an on-chain PoC showing the permissionless distributor claim defeated it. Redesigned so the fee anchors on balance. The finite, strategy-written relayer allowance is load-bearing: it makes the otherwise-unobservable CoW outflow measurable (`granted - remaining`). Reviewer verified the identity under sweep spam, partial recoveries, partial relayer pulls, inflow-during-swap and a 2000-run fuzz over 12-op sequences; no under-report constructible. ABI break: `approveCowSwap(address,uint256)` deleted and `InitParams.mamoBackend` removed.<br>[`src/MamoMultiMarketStrategy.sol:480`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/MamoMultiMarketStrategy.sol#L480)<br>[`src/MamoMultiMarketStrategy.sol:492`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/MamoMultiMarketStrategy.sol#L492)<br>[`src/MamoMultiMarketStrategy.sol:922`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/MamoMultiMarketStrategy.sol#L922)<br>[`src/MamoMultiMarketStrategy.sol:604`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/MamoMultiMarketStrategy.sol#L604) |
| **Sherlock #49** — `recoverERC20` is an unconditional escape hatch from the compound fee | High | Reviewer enumerated every reward-token exit route (fee transfer, recoverERC20, relayer transferFrom) and confirmed the trailing settle is load-bearing. Mutation: dropping the override kills a dedicated test.<br>[`src/MamoMultiMarketStrategy.sol:302`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/MamoMultiMarketStrategy.sol#L302)<br>[`src/MamoMultiMarketStrategy.sol:313`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/MamoMultiMarketStrategy.sol#L313)<br>[`src/BaseStrategy.sol:70`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/BaseStrategy.sol#L70) |
| **Author-1** — `updatePosition()` bricked on a zero-balance strategy after `deactivateMarket` | Medium | Found by the author, not by the audit. The pre-existing test funded the strategy first, which is exactly the branch that hid it.<br>[`src/MamoMultiMarketStrategy.sol:405`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/MamoMultiMarketStrategy.sol#L405) |
| **Merge-73** — Cross-PR merge with #73 produces unmarked compile breaks and a silent semantic resolution | Medium | Resolved along the reviewer's suggested lines at merge commit fea5130 (parents d15d1b5 = #73, a3d9649 = the staking/oracle line). Storage is safe in both directions (#73 adds no state, #74 appends five slots, append-only), which is load-bearing since 013 upgrades a live proxy. Release ordering — 013's backfillPairCount must execute before any removeTokenConfiguration on the upgraded proxy — remains an operational requirement neither proposal encodes.<br>[`src/SlippagePriceChecker.sol:143`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/SlippagePriceChecker.sol#L143)<br>[`src/SlippagePriceChecker.sol:295`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/SlippagePriceChecker.sol#L295)<br>[`src/SlippagePriceChecker.sol:321`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/SlippagePriceChecker.sol#L321)<br>[`test/MoonwellMorphoStrategy.integration.t.sol:1860`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/test/MoonwellMorphoStrategy.integration.t.sol#L1860) |
| **Review New-Medium (dust gate)** — 20 wei of donated reward token re-blocks every CoW order | Medium | The in-source comment claiming an attacker could not block orders with a few wei was simply wrong and was removed (the correction is recorded in-source at 592). Reviewer verified the narrowing does not reopen #32/#49: `allowance <= rewardFeeCharged` is an invariant outside the legacy state (sweepRewardFees is the sole allowance writer, line 506), so a non-legacy strategy can never satisfy the predicate and a legacy strategy leaves that state only through a sweep, which charges the fee. Three mutants killed including `>` -> `>=`. Two accepted residuals noted: in the legacy state a <=19 wei balance still signs untaxed (bps rounding, un-amortisable at ~50k gas), and a positive-rebasing reward token could re-trigger the gate (liveness only; rebasing documented unsupported).<br>[`src/MamoMultiMarketStrategy.sol:604`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/MamoMultiMarketStrategy.sol#L604)<br>[`src/MamoMultiMarketStrategy.sol:605`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/MamoMultiMarketStrategy.sol#L605) |
| **Sherlock #31 / #46 / #48 / MOO-725** — Factory onboarding bricks: pinned implementation, immutable splits, inactive markets in `initialize` | Medium | All three validated by the reviewer as fully closed; `testAddMarketThenCreateStillWorks` confirms #46's brick was not reintroduced by the later setter tightening, and MarketRegistry is append-only (no removeMarket) so stored length can never exceed market count after a valid set.<br>[`src/MultiMarketStrategyFactory.sol:129`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/MultiMarketStrategyFactory.sol#L129)<br>[`src/MultiMarketStrategyFactory.sol:116`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/MultiMarketStrategyFactory.sol#L116)<br>[`src/MultiMarketStrategyFactory.sol:191`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/MultiMarketStrategyFactory.sol#L191)<br>[`src/MamoMultiMarketStrategy.sol:231`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/MamoMultiMarketStrategy.sol#L231) |
| **Sherlock #36 / MOO-738** — Deposits routed to the wrong market after `deactivateMarket` leaves an incomplete allocation | Medium | Reviewer confirmed no path leaves an inactive market holding a nonzero split. Correctness improved at a liveness cost — see the deactivation-window residual below.<br>[`src/MamoMultiMarketStrategy.sol:656`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/MamoMultiMarketStrategy.sol#L656) |
| **Validation-1** — Max withdrawal reverts by one unit against live MetaMorpho (regression introduced by the Lower-7 fix) | Medium | Fix validated by three mutations, all killed. Notable process point: the reviewer's stated 0/100-vs-50/50 repro dispute was adjudicated — see the separate refuted entry — and the fork test deliberately uses 0/100 because a 50/50 fork test against latest is block-dependent (flaky), while 0/100 reverts pre-fix at every block probed. Size cost noted: implementation grew 21,904 -> 22,488 runtime bytes.<br>[`src/MamoMultiMarketStrategy.sol:790`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/MamoMultiMarketStrategy.sol#L790)<br>[`src/MamoMultiMarketStrategy.sol:850`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/MamoMultiMarketStrategy.sol#L850) |
| **Validation-CI-regression** — `_isMarketTarget` reverted for un-migrated v1 proxies (regression from Lower-5) | Medium | Caught by CI (test/ERC20StrategyV2.t.sol::testBackendCanClaimRewards, which upgrades a real mainnet proxy without migrating) after the author reported the previous round green from local runs only. Reviewer proved the window from storage layout rather than sampling: marketRegistry is at slot 60 and v1 ended at slot 59, so marketRegistry == 0 on every live v1 proxy with certainty; mToken/metaMorphoVault are never written by this implementation, so they are unforgeable relics an attacker cannot use to steer the branch. Four mutations all caught.<br>[`src/MamoMultiMarketStrategy.sol:969`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/MamoMultiMarketStrategy.sol#L969) |
| **Review Lower-1** — `setDefaultSplitBps` under-validates — truncated array or nonzero split on an inactive market | Low | Left on BACKEND_ROLE rather than admin-gated, deliberately, since it is the operational repair path. Reviewer's first pass found the inactive-market half unpinned (deleting the loop left the suite green); a test was added in the next round and mutation-confirmed. Unremarked residual: if every market for a token is deactivated the setter becomes unsatisfiable (all entries zero, sum 10000), so creation stays bricked until reactivateMarket.<br>[`src/MultiMarketStrategyFactory.sol:225`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/MultiMarketStrategyFactory.sol#L225)<br>[`src/MultiMarketStrategyFactory.sol:230`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/MultiMarketStrategyFactory.sol#L230) |
| **Review Lower-2** — `claimRewards` batch poisoning — one unpriced or underlying-denominated reward reverts the whole batch | Low | Reviewer verified `_isSettleableRewardToken` (952) is the exact negation of `_requireRewardToken`'s (944) three requires, so a settleable token can never be skipped. Nit accepted: skipped entries emit feeAmounts[i] == 0, indistinguishable from 'nothing owed'.<br>[`src/MamoMultiMarketStrategy.sol:450`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/MamoMultiMarketStrategy.sol#L450) |
| **Review Lower-3** — `migrateV1ToMarketRegistry` lets a fresh factory proxy's owner point at an arbitrary registry | Low | Mutation-verified. Reviewer note: the test's 'hostile' registry is a real empty MarketRegistry, so with the require removed it fails on the split-total check rather than demonstrating the spoof — it pins the guard without demonstrating the exploit. Residual, unchanged from pre-fix: a v1 strategy's owner can still front-run the operator and point their own strategy at a fabricated registry (self-harm only, one-shot, no admin override).<br>[`src/MamoMultiMarketStrategy.sol:263`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/MamoMultiMarketStrategy.sol#L263) |
| **Review Lower-4** — Underflow panic in `_withdrawProRata` if a market pays out more than requested | Low | Reviewer endorsed keeping the clamp rather than reverting: the excess stays idle and is bounded by the trailing require at :350 (withdraw path). Unpinned by test (needs a non-standard registered market) — assessed as proportionate.<br>[`src/MamoMultiMarketStrategy.sol:722`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/MamoMultiMarketStrategy.sol#L722)<br>[`src/MamoMultiMarketStrategy.sol:727`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/MamoMultiMarketStrategy.sol#L727)<br>[`src/MamoMultiMarketStrategy.sol:890`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/MamoMultiMarketStrategy.sol#L890) |
| **Review Lower-6** — Factory trusts whatever implementation `latestImplementationById` returns | Low | Cannot brick creation — all three compare against the factory's own immutables, which are the values passed into initialize. Reviewer noted the asserts are unpinned (deleting all three leaves the factory unit suite green) and reintroduce a weak coupling the factory's own natspec was designed to avoid: a future implementation renaming any of those getters takes creation offline.<br>[`src/MultiMarketStrategyFactory.sol:158`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/MultiMarketStrategyFactory.sol#L158)<br>[`src/MultiMarketStrategyFactory.sol:159`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/MultiMarketStrategyFactory.sol#L159)<br>[`src/MultiMarketStrategyFactory.sol:160`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/MultiMarketStrategyFactory.sol#L160) |
| **Review Lower-7** — `_withdrawUpTo` assumed `withdraw(maxWithdraw(this))` is always satisfiable | Low | The first attempt at this fix (redeem(maxRedeem)) introduced a live-reproducible regression — see the max-withdrawal entry. Final form redeems the whole share balance when the vault can honour it, which is exactly the quantity `_getTotalBalance` advertises.<br>[`src/MamoMultiMarketStrategy.sol:790`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/MamoMultiMarketStrategy.sol#L790) |
| **Review Lower-8** — `BACKEND_ROLE` re-derived locally instead of cross-checked against the registry | Low | A typed call broke `testRevertIfAddStrategyWithIncorrectRegistry` (which deliberately initializes against an EOA registry) by replacing the registry's precise diagnosis with FailedCall(); hence a raw staticcall. The first version failed open for anything unanswerable, which the reviewer showed was wider than its justification — MamoLeveragedAeroStrategyFactory and MamoStakingStrategyFactory carry no equivalent assert, so for those strategies this read is the only defense. Narrowed to codeless-only in adde455.<br>[`src/MultiMarketStrategyFactory.sol:95`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/MultiMarketStrategyFactory.sol#L95)<br>[`src/BaseStrategy.sol:146`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/BaseStrategy.sol#L146)<br>[`src/BaseStrategy.sol:147`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/BaseStrategy.sol#L147) |
| **Review Lower-9** — Split-authority inconsistency between factory and strategy | Low | Settled as a side effect of the #41 fix; reviewer confirmed the two are now the same predicate. The widening (every BACKEND_ROLE holder) is disclosed in the spec doc as accepted.<br>[`src/BaseStrategy.sol:50`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/BaseStrategy.sol#L50)<br>[`src/MultiMarketStrategyFactory.sol:243`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/MultiMarketStrategyFactory.sol#L243) |
| **Sherlock #38 / MOO-739** — `convertToAssets` over-reports ERC4626 value because it excludes withdrawal fees | Low | Straight substitution; verified by grep over src/.<br>[`src/MamoMultiMarketStrategy.sol:881`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/MamoMultiMarketStrategy.sol#L881) |
| **Validation-2** — `clearRewardToken` reopens a narrow #49 escape via a stale anchor | Low | PoC-confirmed by two independent passes; post-fix an end-to-end sequence collects 97.5e18 vs 50e18 pre-fix. The `!_isMarketTarget` exclusion is load-bearing — dropping it lets recoverERC20 take 25e18 out of a live market position — and gained its own test in adde455. Scheme-independent w.r.t. PR #74 because `_settleRewardFees` makes no price-checker call (confirmed: no SlippagePriceChecker read between lines 492 and 507).<br>[`src/MamoMultiMarketStrategy.sol:313`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/MamoMultiMarketStrategy.sol#L313)<br>[`src/MamoMultiMarketStrategy.sol:492`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/MamoMultiMarketStrategy.sol#L492) |

### Staking & oracle

| Finding | Sev | How it was fixed |
|---|---|---|
| **New-1** — setStakingRegistry's authorization gate read the storage slot it writes, making the first use irrevocable | High | Every other capability that role holds is revocable; this one was not. Recovery would have needed a fresh implementation plus per-strategy upgradeStrategy opt-ins from all 3,846 owners — recreating the exact #35 delivery dead-end setStakingRegistry exists to escape. The owner arm is additive: it does not gate, delay or reorder the batched admin migration, so it is not the owner veto the author had rightly rejected.<br>[`src/MamoStakingStrategy.sol:180`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/MamoStakingStrategy.sol#L180)<br>[`src/MamoStakingStrategy.sol:182`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/MamoStakingStrategy.sol#L182)<br>[`src/MamoStakingStrategy.sol:162`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/MamoStakingStrategy.sol#L162) |
| **Sherlock #33 / MOO-733** — compound() trusted the DEX router's reported output and left a standing allowance | High | Author declined the router allow-list half of the recommendation: the admin is the multisig, so an allow-list would place a second gate in front of the same key holder. Reviewer confirmed the per-swap (not global) snapshot is the correct choice, since a global snapshot would let one iteration's surplus satisfy another's floor.<br>[`src/MamoStakingStrategy.sol:464`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/MamoStakingStrategy.sol#L464)<br>[`src/MamoStakingStrategy.sol:469`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/MamoStakingStrategy.sol#L469)<br>[`src/MamoStakingStrategy.sol:481`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/MamoStakingStrategy.sol#L481)<br>[`src/MamoStakingRegistry.sol:211`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/MamoStakingRegistry.sol#L211) |
| **N3** — The rewritten heartbeat / zero-answer / incomplete-round guards were untested on the implementation this PR ships | Medium | Reviewer's finding reproduced exactly by deleting each require and watching all three existing tests stay green; the new test fails on each.<br>[`test/SlippagePriceChecker.integration.t.sol:626`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/test/SlippagePriceChecker.integration.t.sol#L626)<br>[`src/SlippagePriceChecker.sol:547`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/SlippagePriceChecker.sol#L547) |
| **New-2** — A replacement registry's defaultSlippageInBps escaped the 2500 bps cap | Medium | Additive to the known malicious-checker path: it drives the floor to zero with no attacker-authored contract on chain for monitoring to notice. Bounding against the candidate's own claimed cap would be circular, so a strategy-side constant is used.<br>[`src/MamoStakingStrategy.sol:235`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/MamoStakingStrategy.sol#L235)<br>[`src/MamoStakingStrategy.sol:48`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/MamoStakingStrategy.sol#L48) |
| **New-3** — The MAMO principal bound rested on an accidental unchecked-subtraction underflow | Medium | MamoStakingRegistry forbids listing MAMO but an arbitrary contract is bound by no such rule. The prior bound was correct but accidental, unstated, and one refactor away from being optimised into a silent loss.<br>[`src/MamoStakingStrategy.sol:426`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/MamoStakingStrategy.sol#L426)<br>[`docs/MAMO_STAKING_ARCHITECTURE.md:560`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/docs/MAMO_STAKING_ARCHITECTURE.md#L560) |
| **New-4** — setStakingRegistry probed 2 of the 8 selectors the strategy reads at runtime | Medium | Needs no malice — an honest v3 registry renaming or dropping any of those selectors would pass a two-selector probe and brick the fleet. Raw staticcalls throughout because a typed call to a missing selector reverts in this frame with empty data, indistinguishable from an internal bug.<br>[`src/MamoStakingStrategy.sol:201`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/MamoStakingStrategy.sol#L201)<br>[`src/MamoStakingStrategy.sol:219`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/MamoStakingStrategy.sol#L219)<br>[`src/MamoStakingStrategy.sol:243`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/MamoStakingStrategy.sol#L243) |
| **Sherlock #34 / MOO-748** — Cascading decimal truncation in getExpectedOutFromChainlink under-quoted multi-hop prices | Medium | The finding's exact numeric example now returns 2.01e18 (was 2.00e18) and rejects the 1.985 order; mixed reversed/non-reversed feeds are commutative; overflow headroom ~24 orders of magnitude above any real supply; storage layout append-only.<br>[`src/SlippagePriceChecker.sol:562`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/SlippagePriceChecker.sol#L562)<br>[`src/SlippagePriceChecker.sol:573`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/SlippagePriceChecker.sol#L573) |
| **Sherlock #39 / MOO-726** — isRewardToken answered from a permanently latched legacy flag, not live pair configuration | Medium | Both halves of the latch fixed plus a migration path, because the nested pair mapping is not enumerable on chain. Reviewer fuzz-verified the counter invariant over 256 add/remove sequences with no underflow or desync.<br>[`src/SlippagePriceChecker.sol:431`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/SlippagePriceChecker.sol#L431)<br>[`src/SlippagePriceChecker.sol:101`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/SlippagePriceChecker.sol#L101)<br>[`src/SlippagePriceChecker.sol:258`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/SlippagePriceChecker.sol#L258)<br>[`src/SlippagePriceChecker.sol:321`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/SlippagePriceChecker.sol#L321) |
| **R-item4** — compound() emitted the requested balance as amountIn, not what the router actually pulled | Low | Both amounts in CompoundRewardTokenProcessed are now measured balance deltas rather than router-reported figures, so neither an under-pulling nor an over-reporting router can drift reconciliation.<br>[`src/MamoStakingStrategy.sol:478`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/MamoStakingStrategy.sol#L478) |
| **Sherlock #44 / MOO-734** — SlippagePriceChecker implementation left uninitialized and claimable | Low | Reviewer empirically confirmed the implementation reverts InvalidInitialization(). The finding's other named contract (MamoMultiMarketStrategy) belongs to sibling PR #73, so #44 is only fully covered once both merge.<br>[`src/SlippagePriceChecker.sol:143`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/SlippagePriceChecker.sol#L143) |
| **Sherlock #50 / MOO-744** — compound() computed its swap deadline as block.timestamp + 300, which is tautological | Low | The original fix was untestable — restoring the vulnerability left all 124 tests green because the happy path ran on a strategy with no accrued rewards and the swap loop was skipped. A MockConfigurableSwapRouter recording the deadline, driven by a real accrued cbBTC reward, now discriminates. The upper bound closes compound(type(uint256).max) silently restoring the tautology.<br>[`src/MamoStakingStrategy.sol:403`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/MamoStakingStrategy.sol#L403)<br>[`src/MamoStakingStrategy.sol:404`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/MamoStakingStrategy.sol#L404)<br>[`src/MamoStakingStrategy.sol:405`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/MamoStakingStrategy.sol#L405) |
| **83dbba8** — Two WETH price-check tests depended on live feed freshness and failed CI non-deterministically | Informational | Reviewer independently mutation-proved the tests were not weakened: deleting the price gate in GPv2OrderChecks.sol makes both tests fail. The freshener keeps the live answer so the arithmetic under test stays production arithmetic, and tests that care about feed timing install their own mocks which override it.<br>[`test/SlippagePriceChecker.integration.t.sol:101`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/test/SlippagePriceChecker.integration.t.sol#L101) |
| **Sherlock #43 / MOO-745** — Dead defaultSlippageInBps on MamoStakingStrategyFactory | Informational | No queued-calldata impact — the factory is an EOA deployment, not a Safe action.<br>[`src/MamoStakingStrategyFactory.sol:23`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/MamoStakingStrategyFactory.sol#L23) |

### Cross-cutting

| Finding | Sev | How it was fixed |
|---|---|---|
| **Validation-(tenderly)** — The tightened oracle default broke `make tenderly-harness` | Medium | Fixed in the harness rather than by refreshing feeds, because script/tenderly/lib/market.sh deliberately does not implement feed repointing for LPV2. The matrix's stale-oracle scenario still fires because it warps 27 h, past the 24 h ceiling, and its comment now says so for the right reason; README §5 rewritten. Reviewer validated from source (no vnet reachable).<br>[`script/tenderly/LPV2TenderlyHarness.s.sol:260`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/script/tenderly/LPV2TenderlyHarness.s.sol#L260) |

## Not remediated

Accepted residuals, partial fixes and deferred decisions, each with the reasoning and the compensating control. Listed so the disposition is reviewable rather than implicit.


### LP / Aerodrome

| Finding | Sev | Status | Why not, and what protects us |
|---|---|---|---|
| **R1-residual** — Permissionless EIP-1271 order placement still leaks up to rebalanceSlippageBps of the approved principal per cycle | Medium | accepted-residual | Bounded on all three axes and each bound was checked in source: size by the exact-amount `forceApprove(VAULT_RELAYER, sellAmount)`, direction by the module's `sellToken == sellTokenInFlight()` require, price by the 50 bps Chainlink-referenced floor wired in 011. A third-party fill is therefore economically a fair-priced version of the swap the operator intended; the operational response is to recompute remaining excess and proceed to rebuild, not to unwind. Closing it entirely needs a per-order nonce/authorization scheme the module does not have, which is a design addition rather than a fix. Reasonable to accept at 50 bps; would not be reasonable at the old shared 200 bps, which is why the knob shipped.<br>[`docs/superpowers/specs/2026-07-02-lp-auto-balancer-v2-swap-rebalance-design.md:144`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/docs/superpowers/specs/2026-07-02-lp-auto-balancer-v2-swap-rebalance-design.md#L144)<br>[`src/LPCompoundModule.sol:51`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/LPCompoundModule.sol#L51) |
| **R2** — Two-transaction USD value floor compares across transactions; a mid-flight basket move can wedge the rebuild or mask real loss | Medium | accepted-residual | Defensible because the failure mode costs downtime, not principal: mid-flight funds sit as loose tokens with no LP exposure and no IL, and `rebuildAfterSwap` is retriable indefinitely (no cooldown on the rebuild leg — the cooldown is stamped only on success). Compensating controls verified: both tolerance knobs are admin-only and hard-capped at 500 bps, `exit()` is an unconditional escape, and the backend logs realized execution shortfall (fill vs Chainlink mid) per cycle so leakage is measured off-chain rather than inferred from a floor that is known to be blind in the masking direction. A reader should note the masking half is the weaker acceptance: up to 400 bps of real execution loss can pass unnoticed on-chain, and the only thing catching it is the off-chain shortfall log — so this acceptance is contingent on that log actually being wired.<br>[`docs/superpowers/specs/2026-07-02-lp-auto-balancer-v2-swap-rebalance-design.md:146`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/docs/superpowers/specs/2026-07-02-lp-auto-balancer-v2-swap-rebalance-design.md#L146)<br>[`docs/superpowers/specs/2026-07-03-lp-auto-balancer-v2-backend-spec.md:497`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/docs/superpowers/specs/2026-07-03-lp-auto-balancer-v2-backend-spec.md#L497) |
| **R3-residual** — Oracle/sequencer probe at unwind bounds the state at unwind, not at rebuild — and the tighter 3600s bound made the strand window ~26x more reachable | Medium | accepted-residual | Closing it would require an oracle-free rebuild path — minting and clearing the value floor with no price — which is strictly worse than a stranded retry. Funds are never locked: `exit()` is oracle-free, so the worst case is unstaked principal earning nothing until a feed refreshes. The frequency increase is the part a reader should weigh: the team traded a 26x more reachable liveness stall for a staleness bound that is 3 heartbeats instead of ~76 (measured basis: 1232 s max inter-round gap over 60 rounds of each configured Base feed), which is the right direction for a price-sensitive mint. Compensating controls are operational only — short `validTo`, preferring the atomic no-swap `rebalanceUsingAlt` while a feed is degraded, and an operator rule to freeze oracle config during a cycle — and the third stranding case (a Safe config write mid-flight) has no on-chain block at all, so it rests entirely on that operator rule.<br>[`src/LPAutoBalancerV2.sol:762`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/LPAutoBalancerV2.sol#L762)<br>[`src/LPAutoBalancerV2.sol:773`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/LPAutoBalancerV2.sol#L773) |
| **R4** — COMPOUND_APP_DATA placeholder has no registered preimage — launch blocker deferred to ops | Medium | deferred | Deferred rather than fixed because no bytecode can create an orderbook document — the registration is an off-chain PUT. It fails safe in two independent ways, both checked: an unregistered appData means the order is rejected at placement, and the unconfigured checker pair makes `checkPrice` revert on-chain even if an order somehow reached settlement. So the swap path is dark, not mispriced. The acceptance rests on the §7 startup invariant (`GET /app_data/{hash}` must return 200) actually being asserted before every swap cycle; if that check is skipped the failure is a loud placement rejection rather than a silent loss, which is why deferring is reasonable.<br>[`docs/superpowers/specs/2026-07-02-lp-auto-balancer-v2-swap-rebalance-design.md:150`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/docs/superpowers/specs/2026-07-02-lp-auto-balancer-v2-swap-rebalance-design.md#L150)<br>[`multisig/mamo-multisig/011_LPAutoBalancerV2Setup.sol:99`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/multisig/mamo-multisig/011_LPAutoBalancerV2Setup.sol#L99) |
| **Review #3 residual** — The unwind probe bounds the state at unwind, not at rebuild — a feed going stale, a sequencer restart, or a mid-flight admin config write between the two transactions still strands the rebuild | Medium | accepted-residual | Not closable in this contract: closing it would require an oracle-free rebuild path — minting and clearing the value floor with no price — which is strictly worse. Compensating controls: funds are never locked (exit() is oracle-free and the order can be retried), a short CowSwap validTo, and preferring the no-swap rebalanceUsingAlt while either feed is degraded.<br>[`src/LPAutoBalancerV2.sol:762`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/LPAutoBalancerV2.sol#L762)<br>[`docs/superpowers/specs/2026-07-03-lp-auto-balancer-v2-backend-spec.md:267`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/docs/superpowers/specs/2026-07-03-lp-auto-balancer-v2-backend-spec.md#L267) |
| **Sherlock #41** — `getBackendAddress()` returns `getRoleMember(BACKEND_ROLE, 0)` — an identity that moves on unrelated membership edits | Medium | partially-remediated | The registry itself cannot be fixed (not upgradeable, no proxy, and `BaseStrategy.mamoStrategyRegistry` has no setter) — the author originally recorded the whole finding as accepted risk on that basis. The reviewer showed the premise was false for #41: every consumer is in freshly deployed strategy code. Residual: the fix lives in the implementation, not the deployed proxies; `MamoStrategyRegistry.upgradeStrategy` requires `isUserStrategy(msg.sender, strategy)`, so upgrades are owner-initiated and #41 stays live on every un-upgraded proxy indefinitely with no admin override. Accepted consequence of the fix: the authorized set widens from one index-0 principal to every BACKEND_ROLE holder, materially adding the MAMO_BACKEND EOA which gains fleet-wide `setFeeRecipient` on upgraded strategies (this widening is disclosed in-source at BaseStrategy.sol:46).<br>[`src/BaseStrategy.sol:50`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/BaseStrategy.sol#L50)<br>[`src/MamoMultiMarketStrategy.sol:179`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/MamoMultiMarketStrategy.sol#L179) |
| **Sherlock #47 / MOO-732** — Factory hardcodes the backend address as an immutable | Medium | partially-remediated | DOWNGRADED from remediated. The code half is genuinely fixed and verified. But the finding's own evidence is that the live registry does not satisfy the new gate, and the remedy for that lives in multisig/mamo-multisig/011 — a proposal script that has not executed on chain. Until it does, the role-based factory cannot onboard strategies through the operator EOA, so the finding is not closed end-to-end. Validate() asserts the grant at 334-335, which pins the intent but not the on-chain state.<br>[`src/MultiMarketStrategyFactory.sol:243`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/MultiMarketStrategyFactory.sol#L243)<br>[`src/MultiMarketStrategyFactory.sol:54`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/MultiMarketStrategyFactory.sol#L54) |
| **aero-deleverage-oracle** — Deleverage fails closed on a stale our-feed while Moonwell liquidation continues against its own oracle | Medium | accepted-residual | The fail-closed direction is the correct one and that is the crux: deleveraging at a stale or manipulated price realizes losses against a bad price and could itself be the attacker's goal, so refusing is safer than acting. Closing the window would require trusting Moonwell's oracle for our own health math, importing their oracle's risk into every one of our paths — a strictly larger dependency for a narrow benefit. Verified compensating control: `deleverage` is permissionless below `minHealthBps`, so anyone can act the instant our feed refreshes, and it repays only to a small buffer above the minimum rather than running the full LTV gate, which keeps the recovery cheap and repeatable. Residual exposure is the liquidation penalty on positions that cross Moonwell's threshold during the stale window — real, bounded by how long a Base Chainlink feed can stall, and not reducible without the oracle-trust tradeoff above.<br>[`src/leveraged-aero/LeveragedAeroManager.sol:468`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/leveraged-aero/LeveragedAeroManager.sol#L468)<br>[`src/leveraged-aero/LeveragedAerodromeCLStrategy.sol:806`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/leveraged-aero/LeveragedAerodromeCLStrategy.sol#L806) |
| **R5-remainder** — In-flight guards still missing on rebalanceUsingAlt / stake / withdrawPosition / deregisterPosition — queued for the next rev | Low | deferred | The missing guards are ergonomic rather than safety-critical, and that claim is checkable: all four functions read valuation/position state before any teardown or mint, so the burned-NFT read reverts first and there is no partial-state path to a double-mint. All four are role-gated (`DEFAULT_ADMIN_ROLE` for the two withdrawals, `REBALANCER_ROLE` for `stake`), so the confusing revert is seen by the operator, never the public. The one substantive point a reader should weigh is that `withdrawPosition`/`deregisterPosition` LOOK like escapes and are not — they leave `inFlight` set and the relayer approval live — so the acceptance depends on operator discipline that `exit()` is the single escape. Deferral is driven by EIP-170 headroom, and the doc's own 621 B figure is stale-by-policy (it says to re-measure), so the budget argument should be re-checked before the next rev.<br>[`src/LPAutoBalancerV2.sol:1408`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/LPAutoBalancerV2.sol#L1408)<br>[`src/LPAutoBalancerV2.sol:1047`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/LPAutoBalancerV2.sol#L1047) |
| **R6** — Misc accepted low-severity items: standing max AERO relayer approval, mid-flight config setters rebasing the floor, cooldown burned by an unfilled cycle, $0.01 alt dust threshold | Low | accepted-residual | Each item is separately bounded and I checked the bound. The AERO approval covers reward-only exposure and never touches principal, mitigated by the operator rule not to let AERO pool on the module — the exposure is literally whatever AERO is sitting on the module at any instant. The mid-flight config setters are `DEFAULT_ADMIN_ROLE`-gated (verified on `setMaxOracleDelays` at line 465) and `setPool` is already hard-blocked, so the residual is a Safe-operator discipline requirement, not an attacker surface. A burned cooldown is a pure liveness cost. The dust threshold cannot cause loss, only dust-sized alt mints, and it is constrained from below by the asserted MIN_ALT_VALUE_USD >= MIN_MAIN_LEG_USD ordering. Grouping four unrelated items under one id is the weak part of this entry — the config-setter item is the only one with a real operator-error path and would be easier to track separately.<br>[`docs/superpowers/specs/2026-07-02-lp-auto-balancer-v2-swap-rebalance-design.md:154`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/docs/superpowers/specs/2026-07-02-lp-auto-balancer-v2-swap-rebalance-design.md#L154)<br>[`src/LPAutoBalancerV2.sol:74`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/LPAutoBalancerV2.sol#L74) |
| **R6 (design risk register)** — Admin config setters are callable mid-flight and silently rebase the two-transaction floor's basis; standing max AERO relayer approval on the compound module | Low | accepted-residual | Accepted as low with operator rules rather than code: freeze config during a cycle, don't let AERO pool on the module, raise MIN_ALT_VALUE_USD next revision. The mid-flight setter class is also named as member (iii) of the unwindForSwap residual in-source (src/LPAutoBalancerV2.sol:767-768).<br>[`docs/superpowers/specs/2026-07-02-lp-auto-balancer-v2-swap-rebalance-design.md:154`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/docs/superpowers/specs/2026-07-02-lp-auto-balancer-v2-swap-rebalance-design.md#L154) |
| **R7** — Tick commitment pins the tick PAIR, not the branch — at width == 2 × tickSpacing a 1-tick push can convert a balanced mint into a single-sided one | Low | deferred | Deferring is reasonable here specifically because deferral forecloses nothing: both closures are zero-bytecode, so the on-chain surface is identical either way and the decision can be made after phase 1 ships. The choice between them is a yield call — MIN_WIDTH 400 closes it for every caller but doubles every phase-1 position's width against a backtest whose +48%-vs-hold edge comes from tight ranges, whereas the backend simply submitting width ≥ 4 × tickSpacing per cycle closes it today with no Safe write. The interim control is real and verified in the spec (balanced branch forwards both minima untouched), but a reader should note it is a CALLER-discipline control: it protects only callers that size their minima, and the repo's own Tenderly harness passes zeros. So the residual is genuinely open for any caller that copies the harness.<br>[`docs/superpowers/specs/2026-07-02-lp-auto-balancer-v2-swap-rebalance-design.md:156`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/docs/superpowers/specs/2026-07-02-lp-auto-balancer-v2-swap-rebalance-design.md#L156)<br>[`docs/superpowers/specs/2026-07-03-lp-auto-balancer-v2-backend-spec.md:218`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/docs/superpowers/specs/2026-07-03-lp-auto-balancer-v2-backend-spec.md#L218) |
| **R7b** — Residual magnitude inside one committed tick bucket — TickMismatch stays silent while the in-range split swings from 50/50 to ~0.6/99.4 | Low | accepted-residual | Accepted because the on-chain control exists and is complete for a disciplined caller: the balanced branch forwards both mint minima unchanged, so a min sized at `predicted × (1 − MINT_TOLERANCE_BPS/10000)` reverts the sandwich. The division of labour is coherent — `TickMismatch` pins WHERE liquidity lands, the minima pin HOW MUCH of each leg lands there — and neither substitutes for the other. Cost of the attack when minima are zeroed is foregone fees for one cooldown, not principal, which is what makes Low the right severity. The honest caveat is the same as R7's: this is a caller-discipline requirement enforced nowhere in bytecode, and the repo ships a harness that violates it, so 'accepted' is only safe while the production backend is the sole caller.<br>[`docs/superpowers/specs/2026-07-03-lp-auto-balancer-v2-backend-spec.md:246`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/docs/superpowers/specs/2026-07-03-lp-auto-balancer-v2-backend-spec.md#L246)<br>[`docs/superpowers/specs/2026-07-03-lp-auto-balancer-v2-backend-spec.md:375`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/docs/superpowers/specs/2026-07-03-lp-auto-balancer-v2-backend-spec.md#L375) |
| **Review #4 (imthatcarlos)** — The tick commitment does not pin the alt in the balanced branch — spot can move inside the committed bucket | Low | accepted-residual | Deliberately NOT enforced on chain. The side-flip's economic effect is bounded by ~2 x MIN_ALT_VALUE_USD (~$0.02) — reviewer verified the flip point is where both mint residuals vanish, at which point no alt is minted at all — and pinning the side would require adding the alt's own bounds and its 'no alt minted' case to the commitment, which the author declined. The separate in-bucket magnitude channel (value split swinging from 50/50 at spot = floor to ~0.6/99.4 at floor+99; griefing/yield denial, not extraction) is closed operationally by sized mint minima rather than by bytecode, given 90 B of EIP-170 margin.<br>[`src/libraries/LPPositionLib.sol:306`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/libraries/LPPositionLib.sol#L306)<br>[`src/LPAutoBalancerV2.sol:895`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/LPAutoBalancerV2.sol#L895) |
| **Validation-1 (MOO-740 x Review #3 interaction)** — Tightening the staleness bound 26h -> 3600s makes the un-closable unwind/rebuild residual ~26x more reachable | Low | accepted-residual | Accepted knowingly as the price of the MOO-740 fix, and disclosed rather than left implicit. Compensating controls are backend-side: short validTo, prefer the no-swap path while a feed is degraded.<br>[`src/LPAutoBalancerV2.sol:773`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/LPAutoBalancerV2.sol#L773) |
| **Validation-2 / R7** — The tick commitment pins the tick PAIR, not the BRANCH — at width == 2 x tickSpacing a balanced pair and a token1-single-sided pair collide | Low | deferred | Deferred to a product decision rather than fixed. The cheapest closure is config-only and zero-bytecode (MIN_WIDTH 200 -> 400, i.e. minWidth > 2 x maxTickDeviation) but it doubles every phase-1 position's width against a backtest whose +48%-vs-hold edge is tight ranges — a yield call, not a security call. Reviewer verified both the yield argument against the repo and the closure arithmetic (advancing floorAlign by two spacings needs a >=101-tick push, which maxTickDeviation 100 rejects), and added that any per-call width >= 4 x tickSpacing is immune, so the backend can adopt it per cycle with no Safe write. Interim control in force: the balanced branch forwards both caller mint minima untouched, so sized minima revert the skewed mint.<br>[`docs/superpowers/specs/2026-07-02-lp-auto-balancer-v2-swap-rebalance-design.md:156`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/docs/superpowers/specs/2026-07-02-lp-auto-balancer-v2-swap-rebalance-design.md#L156)<br>[`docs/superpowers/specs/2026-07-03-lp-auto-balancer-v2-backend-spec.md:218`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/docs/superpowers/specs/2026-07-03-lp-auto-balancer-v2-backend-spec.md#L218) |
| **aero-nav-excludes-pending-rewards** — MISSED BY THE SWEEP: nav() excludes pending unclaimed AERO gauge rewards, so share pricing understates value between compounds | Low | accepted-residual | Added because the sweep is scoped as a repo-wide residual register and this one is pre-declared upstream yet absent from it. Direction of the error is the mitigating fact: NAV is understated, so deposits mint slightly too many shares and redemptions pay slightly too little — value transfers from existing holders to depositors, never out of the protocol. Magnitude is bounded by rewards accrued since the last `compound`, so it is a function of compound cadence rather than of position size alone, and a depositor timing entry just before a compound captures at most that amount. Accepting is reasonable while compounds are frequent and the alternative (a gauge `earned()` read inside a view NAV used for share pricing) would add an external call on the hottest accounting path and a new manipulation surface; it becomes unreasonable if compound cadence lengthens materially.<br>[`src/leveraged-aero/LeveragedAerodromeCLStrategy.sol:449`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/leveraged-aero/LeveragedAerodromeCLStrategy.sol#L449)<br>[`docs/LEVERAGED_AERO_CL_AUDIT.md:109`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/docs/LEVERAGED_AERO_CL_AUDIT.md#L109) |
| **aero-redeem-sweep-minout-zero** — MISSED BY THE SWEEP: redeem-path residual-leg sweeps pass minOut = 0, relying solely on the aggregate minAssetsOut floor | Low | accepted-residual | Added because the sweep omitted it and its status is weaker than the others here: the audit doc raises it as a question for auditors ('whether the aggregate floor is a sufficient guard') rather than recording a completed acceptance, so a reader should treat it as documented-but-unresolved. The argument for it being tolerable is that the redeemer supplies `minAssetsOut` and the sweeps' output lands in the same USDC total that floor measures, so a redeemer who sizes that floor is protected end-to-end regardless of per-swap pricing. The argument against is that the aggregate floor is coarse — it cannot distinguish a bad sweep price from ordinary IL — and the route is pinned to a single 100-spacing pool with no fallback, so an illiquid or manipulated leg pool degrades the sweep with nothing intermediate to catch it. Severity Low because the redeemer is the only party exposed and holds the floor parameter; it should be re-rated if any caller passes a loose `minAssetsOut`.<br>[`src/leveraged-aero/LeveragedAeroManager.sol:277`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/leveraged-aero/LeveragedAeroManager.sol#L277)<br>[`docs/LEVERAGED_AERO_CL_AUDIT.md:110`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/docs/LEVERAGED_AERO_CL_AUDIT.md#L110) |
| **Validation-3 (constructor bypass)** — The constructor writes both delay slots directly, bypassing _setMaxOracleDelays — the ceiling never validates the default and no event fires at deploy | Informational | accepted-residual | Accepted with 90 B of EIP-170 margin as the stated reason; routing the constructor through the validating setter costs bytecode the contract does not have. The author also conceded on the record that the earlier claim '26h no longer validates against its own ceiling' was wrong about mechanism — the ceiling never gated the default; the constant was simply edited. Compensating control is the unit assertion that DEFAULT_MAX_ORACLE_DELAY <= MAX_ORACLE_DELAY.<br>[`src/LPAutoBalancerV2.sol:296`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/LPAutoBalancerV2.sol#L296) |
| **Validation-EIP170** — LPAutoBalancerV2 ships with 90 bytes of EIP-170 margin | Informational | accepted-residual | Accepted knowingly and stated as the reason several other findings were closed by config, documentation or operator rule instead of bytecode (constructor validation bypass, item-4 enforcement, R7 closure, the conditional unwind probe). Compensating control: .github/workflows/test.yml:48 runs forge build --sizes, which exits non-zero over EIP-170 (re-verified). Guidance recorded: move a body into LPPositionLib (~17.8 KB of room) rather than trying to fit into 90 B.<br>[`src/LPAutoBalancerV2.sol:816`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/LPAutoBalancerV2.sol#L816) |
| **aero-fee-deferral** — Streaming/performance fee is deferred rather than lost when the fee mint cannot execute | Informational | accepted-residual | A deliberate liveness-over-immediacy choice and not a loss: the fee accrues and is collected at the next crystallize point with the high-water mark untouched, so there is neither double-charge nor forgiveness. Prioritizing user operations over fee collection is the right direction — the alternative lets a paused vault or an unreachable feeRecipient block user activity, which converts a fee-plumbing problem into a user-funds problem. The compensating control is the emitted `op` discriminator plus navPre, which tells an off-chain monitor exactly which entrypoint deferred and at what NAV; Informational is correct because the only party bearing risk is the fee recipient. Note the asymmetry is intentional and documented: crystallize is best-effort on user-exit paths and hard-reverting on `compound`/`settle`.<br>[`src/leveraged-aero/LeveragedAerodromeCLStrategy.sol:99`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/leveraged-aero/LeveragedAerodromeCLStrategy.sol#L99)<br>[`src/leveraged-aero/LeveragedAerodromeCLStrategy.sol:773`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/leveraged-aero/LeveragedAerodromeCLStrategy.sol#L773) |
| **drop-aero-offchain** — AERO swap routing for the weekly drop is configured off-chain at drop time, not by a Safe action | Informational | accepted-residual | An architecture property rather than a gap: the routing is per-call calldata by design, so there is no on-chain state that could have been set and was not — which is exactly why it is Informational and why recording it matters (a reviewer reading the proposal's silence on AERO would otherwise report an omission). The compensating control is the documented drop-time runbook step. The only thing a reader should verify independently is the premise that the balancer, not DropAutomation, stakes the position — if that ever inverts, `addGauge` becomes a genuinely missing on-chain step.<br>[`multisig/mamo-multisig/011_LPAutoBalancerV2Setup.sol:76`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/multisig/mamo-multisig/011_LPAutoBalancerV2Setup.sol#L76) |

### Multi-market strategy & registries

| Finding | Sev | Status | Why not, and what protects us |
|---|---|---|---|
| **Review Deploy-Prereq-2** — Legacy relayer allowances: live type-1 proxies sit at anchor 0 with unlimited relayer approval after the upgrade | Medium | accepted-residual | Not fixable in code for already-deployed proxies; handled as a documented rollout step plus the legacy branch of the signature gate. A test etching the stale max allowance was added at the reviewer's request.<br>[`docs/STRATEGY_REGISTRY_SPEC.md:189`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/docs/STRATEGY_REGISTRY_SPEC.md#L189)<br>[`src/MamoMultiMarketStrategy.sol:604`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/MamoMultiMarketStrategy.sol#L604) |
| **Sherlock #37 / MOO-738** — Withdrawals revert despite sufficient strategy assets (4626 leg and mToken leg) | Medium | partially-remediated | Root cause fixed on both legs; the reviewer verified the cash cap is load-bearing today (live MOONWELL_USDC ~90% utilised). Partial because the strict `require(... == 0)` remains for every non-cash Compound error code — deliberately deferred (see the separate residual entry) — and because `_getTotalBalance` still counts mToken `balanceOfUnderlying` uncapped by `getCash` (independently confirmed at src/MamoMultiMarketStrategy.sol:873), so `withdraw(total)` still reverts on an illiquid Moonwell market while `withdrawAll()` works.<br>[`src/MamoMultiMarketStrategy.sol:753`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/MamoMultiMarketStrategy.sol#L753)<br>[`src/MamoMultiMarketStrategy.sol:727`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/MamoMultiMarketStrategy.sol#L727) |
| **Sherlock #42** — Strategy type ID collision — explicit ids do not advance `nextStrategyTypeId` | Medium | accepted-residual | Doc-only by necessity: MamoStrategyRegistry is `AccessControlEnumerable, Pausable` with no proxy and cannot be upgraded, and existing strategies cannot be re-pointed at a replacement registry. An earlier revision of this PR did modify MamoStrategyRegistry.sol and was reverted in 6e53973 (confirmed in the log; the merged diff touches neither MamoStrategyRegistry.sol nor MarketRegistry.sol) so as not to claim a fix that can never ship. Reviewer accepted the disposition as defensible but corrected the doc: 'neither is reversible' was overstated — redeploying identical bytecode to a fresh address and whitelisting it at the displaced id restores the mapping, and proxies/per-call-resolving factories self-heal. Correct fix requires a new registry deployment plus an admin-gated re-point path in BaseStrategy — a migration project, explicitly out of scope.<br>[`docs/STRATEGY_REGISTRY_SPEC.md:60`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/docs/STRATEGY_REGISTRY_SPEC.md#L60)<br>[`docs/STRATEGY_REGISTRY_SPEC.md:93`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/docs/STRATEGY_REGISTRY_SPEC.md#L93) |
| **MOO-746-R1** — The #45 self-enforcement test has a weak fixture and does not kill the mutation | Low | partially-remediated | The test was added in response to the finding but the fixture pathology the author diagnosed for setDEXRouter was not applied here. Flagged in the final validation pass and not addressed in the last commit. Confirmed at HEAD: the address book supplies MAMO_STRATEGY_REGISTRY, so the deployed-bytecode branch is the one taken.<br>[`test/MamoStrategyRegistry.integration.t.sol:152`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/test/MamoStrategyRegistry.integration.t.sol#L152) |
| **N7** — nextStrategyTypeId is 4 while type 4 is already occupied, so whitelistImplementation(impl, 0) hijacks the WETH upgrade target | Low | partially-remediated | Documented in two places but not fixed in the proposal code. 008's own validate() asserts id 3 and so fails simulation — but only if someone simulates before signing.<br>[`docs/STRATEGY_REGISTRY_SPEC.md:93`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/docs/STRATEGY_REGISTRY_SPEC.md#L93)<br>[`multisig/mamo-multisig/008_MamoStakingV2Deployment.sol:155`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/multisig/mamo-multisig/008_MamoStakingV2Deployment.sol#L155) |
| **R-item1** — Clearing the reward-token flag removed the only CoW-allowance revoke path | Low | accepted-residual | CORRECTED rationale. Fixed on the branch, then structurally moot for MamoMultiMarketStrategy: #73 deleted _approveCowSwap there (confirmed absent from src at HEAD), so its relayer allowance is written only by sweepRewardFees at the settled balance. The claim that 'no owner-callable path can grant an unlimited one' is however not repo-wide — LPCompoundModule.approveCowSwap still grants an unlimited relayer allowance gated on isRewardToken and offers no revoke, so a de-configured AERO reproduces the original failure mode there. Admin-gated and mitigated by that module's recoverERC20 escape hatch.<br>[`test/MoonwellMorphoStrategy.integration.t.sol:1860`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/test/MoonwellMorphoStrategy.integration.t.sol#L1860)<br>[`src/LPCompoundModule.sol:115`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/LPCompoundModule.sol#L115) |
| **Review Deploy-Prereq-3** — Fleet deposit-DoS during the market-deactivation window | Low | accepted-residual | Accepted as the correct trade — the alternative is silently mis-routing an entire deactivated market's allocation. Mitigated by a runbook section rather than by code.<br>[`docs/STRATEGY_REGISTRY_SPEC.md:198`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/docs/STRATEGY_REGISTRY_SPEC.md#L198)<br>[`src/MamoMultiMarketStrategy.sol:656`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/MamoMultiMarketStrategy.sol#L656) |
| **Review Lower-10** — `recoverERC20` for a reward token now depends on the fee transfer succeeding | Low | accepted-residual | Accepted deliberately: the alternative — letting recovery proceed when the fee cannot be paid — is exactly the #49 bypass. Reviewer confirmed the in-source note says what is claimed and even names the part easy to omit (recovery depends on the backend, not the owner).<br>[`src/MamoMultiMarketStrategy.sol:296`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/MamoMultiMarketStrategy.sol#L296)<br>[`src/MamoMultiMarketStrategy.sol:519`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/MamoMultiMarketStrategy.sol#L519) |
| **Review Lower-5** — `_requireRewardToken` does not exclude market targets — a mispriced mToken/4626 share could be taxed as a reward | Low | partially-remediated | Partial: the reviewer's suggested union form of `_isMarketTarget` (legacy relics OR registry scan) was explicitly not taken. The two branches are mutually exclusive, so a post-migration blind spot persists — `migrateV1ToMarketRegistry` requires `isMarketActive` only for legacy markets with a nonzero split, so a v1 proxy at splitVault == 0 holding residual shares migrates cleanly with that vault absent from MarketRegistry, after which sweepRewardFees could tax 5% of real principal. Author's stated reason for declining: defence-in-depth against a case needing a second condition (a price-checker entry) to be live, and it touches the same predicate PR #74 rewrites.<br>[`src/MamoMultiMarketStrategy.sol:946`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/MamoMultiMarketStrategy.sol#L946)<br>[`src/MamoMultiMarketStrategy.sol:969`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/MamoMultiMarketStrategy.sol#L969) |
| **Sherlock #44 / MOO-734** — Implementation contracts left uninitialized and claimable | Low | partially-remediated | Partially remediated because the code fix does not reach production without action: the live SlippagePriceChecker implementation 0x413C38B68fe730F2bC30d8Cde965967D1C7BC599 still returns `owner() == address(0)` on Base (re-confirmed by both author and reviewer), so an implementation redeploy plus proxy re-point is an open deployment prerequisite. Impact is bounded — implementation-local storage, no funds, `upgradeToAndCall` is `onlyProxy`.<br>[`src/MamoMultiMarketStrategy.sol:189`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/MamoMultiMarketStrategy.sol#L189)<br>[`src/SlippagePriceChecker.sol:143`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/SlippagePriceChecker.sol#L143) |
| **Validation-2b** — A never-swept reward batch escapes entirely if the token is cleared first | Low | accepted-residual | Handled as an operating rule rather than code. The author declined to make clearRewardToken two-step with a settlement window because that surface is exactly what is contested with PR #74 — deciding it here would prejudge that.<br>[`docs/STRATEGY_REGISTRY_SPEC.md:247`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/docs/STRATEGY_REGISTRY_SPEC.md#L247)<br>[`src/SlippagePriceChecker.sol:295`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/SlippagePriceChecker.sol#L295) |
| **Validation-Deferred-37** — #37 residual — a nonzero non-cash mToken return code still hard-reverts every withdrawal path | Low | deferred | Deferred deliberately. The author's first justification (tolerating a code changes failure semantics) was refuted by the reviewer: a nonzero code means the mToken changed no state, so tolerance would measure a zero delta and still revert on remaining == 0 if funds cannot be found. The justification was rewritten to the reachability argument instead — every non-cash code was enumerated against the live comptroller: TOKEN_INSUFFICIENT_CASH is pre-empted by the getCash cap, COMPTROLLER_REJECTION needs market membership and the strategy never calls enterMarkets, MARKET_NOT_LISTED needs _unsupportMarket (absent), redeem is not pausable on Moonwell. Recorded in-source at 765-767 as a revisit-before condition for any non-Moonwell Compound-v2 market.<br>[`src/MamoMultiMarketStrategy.sol:768`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/MamoMultiMarketStrategy.sol#L768)<br>[`src/MamoMultiMarketStrategy.sol:834`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/MamoMultiMarketStrategy.sol#L834) |
| **multimarket-recover-blocklist** — recoverERC20 for a reward token depends on the fee transfer succeeding; a blocklisting token can suspend the escape hatch | Low | accepted-residual | Accepted because the alternative reopens the exact fee-bypass the fix closed — letting recovery proceed when the fee cannot be paid restores the drain path, since an attacker who can cause the fee transfer to fail would then extract the full amount fee-free. The suspension is temporary and recoverable in one backend transaction (`setFeeRecipient` to a non-blocklisted address), and the affected token stays in the strategy meanwhile rather than being lost, so no principal is at risk — only the timeliness of an escape hatch. Reasonable, with the caveat that it makes the escape hatch depend on the backend being available; if the backend key is the thing being escaped from, this path is blocked until admin action.<br>[`src/MamoMultiMarketStrategy.sol:296`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/MamoMultiMarketStrategy.sol#L296) |
| **Review Lower-11** — Fee semantics broadened — the fee is charged on receipt of any priced balance, donations included | Informational | accepted-residual | Doc-only by design — the broadening is the mechanism that closes #32. Over-collection is the safe direction. Reviewer confirmed the note is accurate.<br>[`src/MamoMultiMarketStrategy.sol:467`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/MamoMultiMarketStrategy.sol#L467) |
| **Review Lower-12** — Hard-coded CoW appData pins schema 1.3.0 with no escape hatch | Informational | accepted-residual | Declined a setter deliberately: a mutable appData lets whoever can change it widen what the strategy signs. Cost documented in-source (rewards stop compounding but stay withdrawable).<br>[`src/MamoMultiMarketStrategy.sol:624`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/MamoMultiMarketStrategy.sol#L624) |
| **Sherlock #45 / MOO-746** — Spec said the backend whitelists strategy implementations; the code requires DEFAULT_ADMIN_ROLE | Informational | partially-remediated | The headline claim is fixed everywhere, and the reviewer's newly-introduced inaccuracy (non-zero typeId 'adds a version to an existing type') was fixed properly rather than softened. The convenience copies inside SLIPPAGE_PRICE_CHECKER.md were not fully re-authored.<br>[`docs/STRATEGY_REGISTRY_SPEC.md:40`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/docs/STRATEGY_REGISTRY_SPEC.md#L40)<br>[`docs/STRATEGY_REGISTRY_SPEC.md:250`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/docs/STRATEGY_REGISTRY_SPEC.md#L250) |
| **Validation-Info-Observability** — `withdrawAll` and `updatePosition` are silently best-effort; `PositionUpdated` reports requested not achieved splits | Informational | accepted-residual | Accepted and documented rather than fixed; adding an achieved-allocation return or event was assessed as additive and non-blocking. Flagged because integrators read the name `withdrawAll`.<br>[`docs/STRATEGY_REGISTRY_SPEC.md:273`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/docs/STRATEGY_REGISTRY_SPEC.md#L273)<br>[`src/MamoMultiMarketStrategy.sol:409`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/MamoMultiMarketStrategy.sol#L409) |

### Staking & oracle

| Finding | Sev | Status | Why not, and what protects us |
|---|---|---|---|
| **MOO-733-R2** — reinvest() retains a 100%-loss path under the same threat model | High | accepted-residual | Out of scope for this branch; documented rather than closed. Reviewer separately refuted the stronger claim that reinvest() can reach staked principal — MAMO is staked before the loop and the destination is validated against the immutable mamoStrategyRegistry.<br>[`src/MamoStakingStrategy.sol:498`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/MamoStakingStrategy.sol#L498)<br>[`docs/MAMO_STAKING_ARCHITECTURE.md:587`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/docs/MAMO_STAKING_ARCHITECTURE.md#L587) |
| **MOO-733-residual** — Compromised backend can still widen the slippage floor to 2500 bps, and reinvest() retains a full-loss path | High | accepted-residual | Accepted under a threat model where the backend key is trusted-but-monitored, and the two remaining paths differ in kind: the slippage knob is a bounded value leak (25% worst case per swap, which is severe but capped and visible), while `reinvest()`'s destination list is only structurally validated, so its bound comes from the `isUserStrategy`/owner/token checks rather than from an amount. Verified compensating controls: the 2500 bps cap is a hard require in the registry, and `setDEXRouter`/`setSlippagePriceChecker` are admin-gated rather than backend-gated, so a compromised backend cannot redirect the router itself. The intended monitor is off-chain reconciliation of `CompoundRewardTokenProcessed`, and its usefulness is real — both amounts are measured balance deltas, not router-reported figures, so an under-pulling or over-reporting router cannot drift it silently. A reader should judge this acceptance as contingent on that monitoring existing and being alerted on; without it, a 25% slippage widening is not self-announcing.<br>[`docs/MAMO_STAKING_ARCHITECTURE.md:581`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/docs/MAMO_STAKING_ARCHITECTURE.md#L581)<br>[`src/MamoStakingRegistry.sol:264`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/MamoStakingRegistry.sol#L264) |
| **MOO-735-scope** — Registry pause does not reach the 3,846 strategies deployed by the deprecated type-2 factory | High | accepted-residual | This is a consequence of the deliberate ownership guarantee (no third party can upgrade a user's proxy) rather than a missing check, which is why it is accepted rather than fixed — but a reader should weigh that the guarantee converts an emergency control into an opt-in one for the large majority of live strategies, and High is the right severity for that. Reaching type 2 would need a second, byte-identical implementation whitelisted under an explicit type id 2 (the same address cannot be whitelisted twice, and an auto-assigned id would overwrite the WETH upgrade target at id 4), so the closure is not free. Compensating controls: the deprecated registry's admin stays live for the migration's duration as the only authority over un-upgraded proxies, and `setStakingRegistry` makes registry-side remediation deliverable to strategies once they upgrade. The acceptance is time-bounded by a migration campaign, so it should be re-reviewed if that campaign stalls.<br>[`docs/MAMO_STAKING_ARCHITECTURE.md:490`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/docs/MAMO_STAKING_ARCHITECTURE.md#L490)<br>[`docs/MAMO_STAKING_ARCHITECTURE.md:518`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/docs/MAMO_STAKING_ARCHITECTURE.md#L518) |
| **MOO-733-R1** — setDefaultSlippage remains a BACKEND_ROLE knob that loosens the minimum-out floor to 25% | Medium | accepted-residual | Accepted deliberately: severity reduction rather than elimination. The compensating control is off-chain reconciliation of CompoundRewardTokenProcessed, whose two amounts are both measured balance deltas.<br>[`src/MamoStakingRegistry.sol:264`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/MamoStakingRegistry.sol#L264)<br>[`docs/MAMO_STAKING_ARCHITECTURE.md:581`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/docs/MAMO_STAKING_ARCHITECTURE.md#L581) |
| **R-item3** — Registry remediation deadlock: every knob needed during an incident was whenNotPaused | Medium | partially-remediated | Deliberate partial fix: the pause-gated BACKEND setter pattern is a repo-wide convention (MamoStrategyRegistry.addStrategy, MarketRegistry.addMarket/deactivateMarket), and diverging on one registry is what gets flagged next round. The atomic batch requirement is documented instead.<br>[`src/MamoStakingRegistry.sol:204`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/MamoStakingRegistry.sol#L204)<br>[`src/MamoStakingRegistry.sol:245`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/MamoStakingRegistry.sol#L245) |
| **Sherlock #35 / MOO-735** — onlyBackend ignored the registry pause, so the guardian's emergency stop did not halt reward processing | Medium | partially-remediated | upgradeStrategy only moves a proxy to latestImplementationById[itsOwnTypeId] and is isUserStrategy-gated, so delivery to the type-2 fleet needs a second byte-identical deployment whitelisted under type 2 plus 3,846 individual owner opt-ins. Explicitly scoped to new strategies and recorded as an accepted residual rather than reported closed. The doc's earlier, impossible remedy ("whitelist this same implementation for type 2") was corrected — whitelistImplementation reverts on a repeat address.<br>[`src/MamoStakingStrategy.sol:99`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/MamoStakingStrategy.sol#L99)<br>[`src/MamoStakingStrategy.sol:97`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/MamoStakingStrategy.sol#L97) |
| **Sherlock #40 / MOO-741** — No L2 sequencer uptime guard and no rejection of future-dated Chainlink rounds | Medium | partially-remediated | CORRECTED status from 'remediated'. The guard alone is inert — nothing in the deployed system calls the setter, so it early-returns on every path. The wiring is test-enforced (deleting the setter call from build() fails the proposal test) but enforcement is against a simulation, not against chain state; Low-5 further records alternative deployment paths that would land this implementation without the setter. Feed identity, description() and startedAt refresh behaviour confirmed on chain.<br>[`src/SlippagePriceChecker.sol:581`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/SlippagePriceChecker.sol#L581)<br>[`src/SlippagePriceChecker.sol:549`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/SlippagePriceChecker.sol#L549) |
| **e6cfb3c** — The WETH pairs' ETH/USD heartbeat (1200s) sits below the feed's own update cadence | Medium | partially-remediated | CORRECTED status from 'remediated': the whole remediation is an unexecuted Safe batch plus repo config. Must ship with the upgrade, not before it — the pairCounted bookkeeping that keeps configuredPairCount honest only exists in the new implementation. Proved both directions on a fork: warping to updatedAt + 1500, the quote reverts before 013 and returns after, at the same instant.<br>[`multisig/mamo-multisig/013_SlippagePriceCheckerOracleHardening.sol:53`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/multisig/mamo-multisig/013_SlippagePriceCheckerOracleHardening.sol#L53)<br>[`multisig/mamo-multisig/013_SlippagePriceCheckerOracleHardening.sol:124`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/multisig/mamo-multisig/013_SlippagePriceCheckerOracleHardening.sol#L124) |
| **seq-grace-vs-maxtime** — Nothing enforces sequencerGracePeriod >= maxTimePriceValid; raising the order lifetime silently reopens the outage replay window | Medium | accepted-residual | Accepted rather than enforced because the two values live in different scopes — `maxTimePriceValid` is per-token while `sequencerGracePeriod` is global — so a cross-check on the token side would have to iterate the token set on every write of the global value. That is a genuine cost, but a reader should note a cheaper partial enforcement exists (revert in `setMaxTimePriceValid` when the new per-token value exceeds the current global grace period, which is a single comparison and covers the direction the doc says is dangerous), so 'cannot enforce' overstates it — the accurate statement is that only the global-side write would remain unchecked. Compensating controls verified: both setters are owner-only (the timelocked Safe), the shipped values are equal so the window is closed today, and the ordering rule is documented at the setter itself rather than only in the architecture doc.<br>[`src/SlippagePriceChecker.sol:316`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/SlippagePriceChecker.sol#L316)<br>[`multisig/mamo-multisig/013_SlippagePriceCheckerOracleHardening.sol:43`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/multisig/mamo-multisig/013_SlippagePriceCheckerOracleHardening.sol#L43) |
| **24h-heartbeat-pairs** — xWELL and MORPHO single-hop quote legs carry a 24h staleness bound with zero slack over the published heartbeat | Low | deferred | Deferred to the next config revision rather than accepted permanently, and the distinction is defensible: this is a feed-configuration property, not a code defect — the bound tracks the feed's own published heartbeat. Measurement confirms neither pair is in the failure mode ETH/USD was (a bound set BELOW the feed's own cadence, causing spurious reverts at 1200s); WELL/USD's measured 53,012s max gap sits well inside 86400. The remaining concern is only the absence of slack: at exactly the published heartbeat, one late round produces a revert, so the identified fix (90000) is the same reasoning that moved ETH/USD from 1200 to 3600. A reader should note this cuts the opposite way from a security argument — raising to 90000 improves liveness and slightly loosens staleness, which is why it is a config decision rather than a fix.<br>[`docs/MAMO_STAKING_ARCHITECTURE.md:735`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/docs/MAMO_STAKING_ARCHITECTURE.md#L735)<br>[`multisig/mamo-multisig/013_SlippagePriceCheckerOracleHardening.sol:56`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/multisig/mamo-multisig/013_SlippagePriceCheckerOracleHardening.sol#L56) |
| **Low-5** — 013 is no longer the only path that can land this implementation | Low | deferred | Operational gate on execution rather than on merge. The author's stated precondition — 'confirm 013 is the only path used to land this implementation' — remains an unverified deployment requirement, and the removal-clearing ordering hazard is fail-closed only because 013 backfills atomically in the same batch.<br>[`multisig/mamo-multisig/013_SlippagePriceCheckerOracleHardening.sol:15`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/multisig/mamo-multisig/013_SlippagePriceCheckerOracleHardening.sol#L15) |
| **MOO-741-R1** — maxTimePriceValid ≤ sequencerGracePeriod is load-bearing and unenforced | Low | accepted-residual | Documentation-only remediation; no on-chain invariant was added. The reviewer flagged the equality as load-bearing and undocumented, and the author closed it with a SECURITY note rather than a require.<br>[`src/SlippagePriceChecker.sol:311`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/SlippagePriceChecker.sol#L311)<br>[`docs/MAMO_STAKING_ARCHITECTURE.md:721`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/docs/MAMO_STAKING_ARCHITECTURE.md#L721) |
| **MOO-741-R2** — The sequencer uptime feed's own staleness is never checked, and setSequencerUptimeFeed does not probe its argument | Low | accepted-residual | Inherent to the pattern and matching ChainlinkReader elsewhere in the repo; accepted and documented so the docs stop claiming more coverage than the code delivers. The setter is owner-only and 013's validate() asserts the value it writes.<br>[`src/SlippagePriceChecker.sol:585`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/SlippagePriceChecker.sol#L585)<br>[`docs/MAMO_STAKING_ARCHITECTURE.md:729`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/docs/MAMO_STAKING_ARCHITECTURE.md#L729) |
| **MOO-741-R3** — Single-hop pairs with a 24h heartbeat can serve a pre-outage answer after grace expires | Low | deferred | Feed-configuration property rather than a code defect, so deferred to a future config revision. Reviewer's round-1 note said 'One single-hop pair'; the corrected count of two is in the doc. Structurally the same zero-slack shape as the ETH/USD bug, but not currently in that failure mode.<br>[`docs/MAMO_STAKING_ARCHITECTURE.md:735`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/docs/MAMO_STAKING_ARCHITECTURE.md#L735) |
| **N1** — Release doc drift after the fourth 013 step was added | Low | partially-remediated | Same drift class as #45. The prose count and the pair count were fixed in the final commit; the enumerated list in the architecture doc was not brought along.<br>[`docs/MAMO_STAKING_ARCHITECTURE.md:705`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/docs/MAMO_STAKING_ARCHITECTURE.md#L705)<br>[`docs/MAMO_STAKING_ARCHITECTURE.md:735`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/docs/MAMO_STAKING_ARCHITECTURE.md#L735) |
| **N2** — The corrected 3600s WETH quote leg now equals sequencerGracePeriod, removing its margin over the outage guard | Low | accepted-residual | Accepted rather than set to 1800: 3600s is 2.9× ETH/USD's measured worst-case gap, and lowering it would reintroduce the reverts the 1200s bound caused. Flagged for revisit if the grace period is ever raised.<br>[`docs/MAMO_STAKING_ARCHITECTURE.md:743`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/docs/MAMO_STAKING_ARCHITECTURE.md#L743) |
| **N4** — 013 step 4 rewrites the two WETH pairs blind, with no pre-state assertion | Low | deferred | Deferred to pre-execution review rather than fixed in code. Live-relevant: the companion 011_LPAutoBalancerV2Setup owner tx draft uses heartbeat: 1200, which would reintroduce exactly this bug if it lands between preparation and signing.<br>[`multisig/mamo-multisig/013_SlippagePriceCheckerOracleHardening.sol:119`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/multisig/mamo-multisig/013_SlippagePriceCheckerOracleHardening.sol#L119) |
| **N5** — testTokenConfigurationMatchesAssetConfig now runs 013 before asserting parity, so it no longer pins current chain state | Low | accepted-residual | Accepted while the release is pending; the reviewer's guidance is to unwind it after 013 executes.<br>[`test/SlippagePriceChecker.integration.t.sol:131`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/test/SlippagePriceChecker.integration.t.sol#L131) |
| **R-item2** — isTokenPairConfigured read the legacy flag — the same two-sources-of-truth shape #39 was about | Low | partially-remediated | The two-sources-of-truth defect is fixed. The residual — remove last pair, re-add it, and the approval gate passes while GPv2OrderChecks can never settle an order — is fail-closed and owner-recoverable, but the failure moved from a loud revert at approval time to a silent stall at settlement time. Documented rather than eliminated.<br>[`src/SlippagePriceChecker.sol:450`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/SlippagePriceChecker.sol#L450)<br>[`src/SlippagePriceChecker.sol:443`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/SlippagePriceChecker.sol#L443) |
| **feed-bounds-unset** — setFeedBounds is left unconfigured on purpose — no min/max answer sanity check on any Chainlink feed | Low | accepted-residual | The classic minAnswer/maxAnswer pin attack requires an aggregator with active finite market bounds; the aggregators behind these feeds report representational limits that no real answer can reach, which makes a configured bound pure operational burden with no security gain — and a wrongly-tightened bound would itself become a liveness hazard, since `_requireAnswerInBounds` reverts the quote. The capability is deployed and one owner transaction away the day such an aggregator appears. Judged reasonable, with the caveat that 'today's aggregators' is an unversioned claim: if a feed is ever repointed to an aggregator with real bounds, nothing on-chain notices, so this acceptance depends on someone re-checking aggregator properties at each feed change.<br>[`src/SlippagePriceChecker.sol:382`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/SlippagePriceChecker.sol#L382)<br>[`src/SlippagePriceChecker.sol:597`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/SlippagePriceChecker.sol#L597) |
| **pause-asymmetry** — BACKEND_ROLE config setters are pause-gated, so remediating through them requires reopening compound() | Low | accepted-residual | Accepted because the alternative — exempting every backend setter from the pause — is a strictly larger surface: those setters are exactly the knobs a compromised backend would reach for, so keeping them inside the pause is the conservative direction. The exposure window is compressible to a single transaction by shipping unpause → fix → pause as one multisig batch, which is a real mitigation but requires the incident responders to know it. Verified that the two knobs most likely to be needed in an incident (`setDEXRouter`, `setSlippagePriceChecker`) are admin-gated and deliberately exempt from `whenNotPaused`, so the common remediation does not hit this at all — that is what keeps it Low.<br>[`docs/MAMO_STAKING_ARCHITECTURE.md:566`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/docs/MAMO_STAKING_ARCHITECTURE.md#L566)<br>[`src/MamoStakingRegistry.sol:264`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/MamoStakingRegistry.sol#L264) |
| **seq-feed-not-probed** — setSequencerUptimeFeed accepts any address without probing it; a wrong address bricks every quote | Low | accepted-residual | The failure mode is fail-closed and fully recoverable: a bad address blocks quotes rather than admitting bad ones, and the owner corrects it in one transaction — so the worst case is a swap-path outage, not a mispriced fill. Verified compensating controls: the setter is owner-only (the timelocked Safe), and proposal 013 asserts the resolved address and the 3600 grace period in `validate()`, so the intended write is checked in simulation before signing. Note the assertion compares against the address-book entry rather than a literal in the proposal, so a wrong entry in addresses/8453.json would pass validation — the real guarantee is the address book, which is where a reviewer should look.<br>[`docs/MAMO_STAKING_ARCHITECTURE.md:732`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/docs/MAMO_STAKING_ARCHITECTURE.md#L732)<br>[`multisig/mamo-multisig/013_SlippagePriceCheckerOracleHardening.sol:166`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/multisig/mamo-multisig/013_SlippagePriceCheckerOracleHardening.sol#L166) |
| **seq-feed-staleness** — The sequencer uptime feed's own updatedAt is not checked — a stalled feed reporting 'up' is believed | Low | accepted-residual | Inherent to Chainlink's documented L2 sequencer-uptime pattern — the feed has no heartbeat and updates only on state transitions, so any staleness bound would be arbitrary and would itself become a liveness hazard during long healthy periods (a healthy sequencer produces no rounds for weeks, which any bound would read as stale and use to block every quote). Consistency with `ChainlinkReader` elsewhere in the repo means one convention rather than two. The per-quote price-feed staleness bounds remain in force independently, so a stalled uptime feed does not by itself admit a stale price — it only removes the outage-specific refusal. That layering is what makes Low correct.<br>[`src/SlippagePriceChecker.sol:581`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/SlippagePriceChecker.sol#L581)<br>[`docs/MAMO_STAKING_ARCHITECTURE.md:729`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/docs/MAMO_STAKING_ARCHITECTURE.md#L729) |
| **setStakingRegistry-pause** — setStakingRegistry is deliberately not pause-gated | Low | accepted-residual | Pause-gating it would create a deadlock class: a registry broken such that `unpause` itself cannot be called would permanently strand every strategy pointing at it, so the exemption buys a real escape at the cost of one more callable-while-paused function. The compensating controls are unusually strong for a Low: a raw-`staticcall` probe of the candidate across the whole runtime surface the strategy reads (slippagePriceChecker, dexRouter, hasRole, BACKEND_ROLE, DEFAULT_ADMIN_ROLE, paused, MAX_SLIPPAGE_IN_BPS, getRewardTokens, matching mamoToken), a strategy-side bound on the candidate's defaultSlippageInBps rather than trusting the candidate's own cap, and — on the admin arm only — a requirement that the caller also hold DEFAULT_ADMIN_ROLE on the candidate. The owner arm is the one to weigh: an owner can migrate their own strategy to any registry passing the probe, which is consistent with the ownership guarantee elsewhere but means the probe is the only barrier.<br>[`src/MamoStakingStrategy.sol:145`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/MamoStakingStrategy.sol#L145)<br>[`docs/MAMO_STAKING_ARCHITECTURE.md:540`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/docs/MAMO_STAKING_ARCHITECTURE.md#L540) |
| **weth-leg-eq-grace** — The corrected WETH quote leg (3600s) now exactly equals the sequencer grace period, contributing no margin over the outage guard | Low | accepted-residual | Accepted deliberately and the tradeoff is legible: 3600s is 2.9x ETH/USD's measured worst-case inter-round gap of 1232s, and lowering it back toward 1800s would reintroduce the spurious reverts the 1200s bound caused — which were mispricing reward swaps, not merely test flakes. The margin being given up was incidental rather than designed (nobody chose 1200 to add 2400s of headroom over the grace period), so losing it is not losing a control. The stated revisit trigger — any future increase to the grace period — is the right one, because that is the change that would make the two diverge in the dangerous direction, and it is the same unenforced-relationship family as `seq-grace-vs-maxtime`.<br>[`docs/MAMO_STAKING_ARCHITECTURE.md:743`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/docs/MAMO_STAKING_ARCHITECTURE.md#L743)<br>[`multisig/mamo-multisig/013_SlippagePriceCheckerOracleHardening.sol:53`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/multisig/mamo-multisig/013_SlippagePriceCheckerOracleHardening.sol#L53) |
| **MOO-745-R1** — Orphaned MAX_SLIPPAGE_IN_BPS constant and stale 'with default slippage' natspec on the factory | Informational | deferred | Deferred, not disputed. Removal would need the reading test updated in the same commit, so it was left out of an audit-remediation branch.<br>[`src/MamoStakingStrategyFactory.sol:17`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/MamoStakingStrategyFactory.sol#L17)<br>[`src/MamoStakingStrategyFactory.sol:143`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/MamoStakingStrategyFactory.sol#L143) |
| **MOO-748-R1** — Final conversion rounds down, which loosens rather than tightens the minimum-out floor | Informational | accepted-residual | Behaviour unchanged, comment corrected. Accepted because the discrepancy is one smallest-unit of the output token, orders of magnitude inside any configured slippage tolerance. Ceil was explicitly declined.<br>[`src/SlippagePriceChecker.sol:566`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/SlippagePriceChecker.sol#L566) |
| **Validation-Deferred-74** — `clearRewardToken` design contradiction with PR #74 — product decision | Informational | deferred | Explicitly not resolved in PR #73: 'a product decision that can't be resolved mechanically — delete clearRewardToken as redundant once #74's zero-semantics land, or make it a real retirement flag'. Status left at deferred because that was #73's own disposition, but the record should note the outcome: #74 landed the union predicate, and clearRewardToken survived with an added `configuredPairCount[fromToken] == 0` precondition (SlippagePriceChecker.sol:295, doc bullet at STRATEGY_REGISTRY_SPEC.md:245). The reviewer's point that the collision would be a loud tripwire rather than a silent no-op is now moot — #74 deleted the tripwire test outright. The strategy side is safe under either scheme because `_settleRewardFees` makes no price-checker call.<br>[`src/SlippagePriceChecker.sol:247`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/SlippagePriceChecker.sol#L247)<br>[`src/SlippagePriceChecker.sol:431`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/SlippagePriceChecker.sol#L431) |
| **spc-force-retire** — Single-call force-retirement of a reward token was not restored — left as a product decision | Informational | deferred | Deferred because restoring force-retire is a semantic addition (new storage slot, new meaning for `isRewardToken`) rather than a defect fix, and the safer two-step retirement is available today. The compensating control is verified in source: the `configuredPairCount == 0` require means an operator reaching for a single-call retire gets a named revert instead of a silently half-retired token whose pairs still quote. Proposal 013 runs `backfillPairCount` for every pre-counter token (line 116, asserted at 184-187), so the count is trustworthy before anyone clears a flag — without that backfill the precondition would be vacuous for legacy tokens, which is the one way this deferral could have been unsafe.<br>[`src/SlippagePriceChecker.sol:284`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/SlippagePriceChecker.sol#L284)<br>[`src/SlippagePriceChecker.sol:292`](https://github.com/moonwell-fi/mamo-contracts/blob/fea51302b610f64ddade261a9d83f1670494bea7/src/SlippagePriceChecker.sol#L292) |

### Cross-cutting

| Finding | Sev | Status | Why not, and what protects us |
|---|---|---|---|
| **R-makefile** — The `test` Makefile target hides failures behind an op-revm panic | Informational | deferred | Verified pre-existing and none of the four files is touched by this PR; confirmed at HEAD that the #74 diff (d15d1b5..fea5130) contains no Makefile change. The reviewer noted the target is currently hiding real failures and worth a one-line Makefile commit.<br>— |

## Raised and refuted

Reviewed, investigated, and found not to be defects.

- **R-013-backfill** — The 013 backfill candidate list is a guess at the live pair set. Retired by the reviewer's own on-chain enumeration: the live set is exactly six pairs (xWELL and MORPHO against USDC, cbBTC, WETH), all inside the cross product, and AERO/cbBTC/WETH/VIRTUALS have no configured pairs at all. backfillPairCount is skip-on-miss so extras are harmless. The residual advice is to re-check immediately before signing in case the LP companion tx lands first.
- **MOO-736 unstake mirror** — Does CLGauge.withdraw() leak LP fees the same way deposit() does?. Not a leak. Closed by tracing the upstream Slipstream functions rather than assuming; recorded as an in-source natspec block naming them. Reviewer explicitly endorsed this as the right way to close a 'mirror this fix' question. No code change.
- **R-corr-1** — Claim that this PR introduced the type-2 compound() incompatibility. Author refuted the attribution and the reviewer accepted it. The conclusion survives: whitelisting for type 2 still does not deliver #35's fix. A minor counter-correction also landed — 0xeC2fa154 is the deprecated staking registry, and strategyTypeId is a BaseStrategy getter the registry reads, not a MamoStrategyRegistry function.
- **R-corr-2** — Claim that the setStakingRegistry admin is an EOA behind a timelock. 0xfE2ff892 is a Gnosis Safe 1.4.1 with 3 owners and threshold 2, plus two enabled modules; there is no transaction guard and no Zodiac Delay on either Safe. Since MAMO_MULTISIG has the identical 3 signers and 2-of-3 threshold, the 'gating on MamoStrategyRegistry's admin would be a genuine escalation' distinction is formally right but practically empty. This is also why the New-1 escape hatch mattered: no timelock stands between a compromised signer set and an irrevocable write.
- **R-refuted-1** — setStakingRegistry grants new capability to redefine the backend or defeat the pause. Refuted by the reviewer on chain: getRoleAdmin(BACKEND_ROLE) and getRoleAdmin(GUARDIAN_ROLE) both return 0x00, so DEFAULT_ADMIN_ROLE can grant itself either, and GUARDIAN_ROLE on the live registry is held by the same address as DEFAULT_ADMIN_ROLE — the pause was never a control against this key. 100% theft of non-MAMO rewards is already reachable via the two existing setters. Recorded so the new surface is not credited with pre-existing exposure.
- **Validation-Refuted-5050** — Author's claim that the 50/50 max-withdrawal configuration 'does not reproduce'. Refuted as stated, though the engineering conclusion stood: 50/50 is block-dependent (reverts at 50094000, passes at 50098000/50100000/50101000), so a 50/50 fork test against latest is flaky, not vacuous; 0/100 reverts at all four blocks and was the right test choice for the wrong stated reason. The reviewer's own 'the 50/50 case is the one that matters' was also too strong. The test comment was rewritten to record the adjudicated version. Incidental finding: at 1000 USDC on one block, 3 of 10 split configurations reverted pre-fix — the bug was broader than either party's model.
- **Validation-Refuted-CI** — cbBTC Price Checker CI failures attributed to this PR's SlippagePriceChecker changes. Confirmed not a code failure: SlippagePriceChecker.integration.t.sol takes the CHAINLINK_SWAP_CHECKER_PROXY branch, which is set in addresses/8453.json, so the suite exercises live on-chain bytecode rather than the locally compiled source — neither the new constructor nor clearRewardToken is reachable. The same test contract backs the green USDC and WETH jobs. Failures were 'failed to get account for 0xBe08A91A…' at gas: 0, an address that appears nowhere in the repo. 27/27 locally.
- **Validation-Refuted-SplitCount** — Claim that the remediation deleted the only test for "Split count exceeds market count". Reviewer's own correction: the test never existed at the merge base either, so nothing was deleted. Adding one was assessed as additive and non-blocking; deliberately not done. Independently re-checked: no test in test/MultiMarketStrategyFactory.unit.t.sol or .integration.t.sol asserts that revert string.
